# Initial Condition for channel flow
Restart
tg.rst

Given
R := 1.0;
U := sin(x)*cos(y)*cos(z);
V := -cos(x)*sin(y)*cos(z);
W := 0.0;
E := (100.0 + 1.0/16.0*(cos(2*x)+cos(2*y))*(cos(2*z)+2))/(1.4-1) + 0.5*(sin(x)*cos(y)*cos(z)*sin(x)*cos(y)*cos(z) + cos(x)*sin(y)*cos(z)*cos(x)*sin(y)*cos(z) );
